server:
  error:
    include-message: always
    include-binding-errors: always
  port: 8080

spring:
  application:
    name: UWBike
  datasource:
    password: ${DBPASS:rita2362}
    url: ${DBURL:jdbc:oracle:thin:@//oracle.fiap.com.br:1521/ORCL}
    username: ${DBUSER:RM554728}
    driver-class-name: ${DBDRIVER:oracle.jdbc.OracleDriver}
  jpa:

    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: ${DBDIALECT:org.hibernate.dialect.OracleDialect}
        format_sql: true
    show-sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
logging:
  level:
    org.flywaydb: debug